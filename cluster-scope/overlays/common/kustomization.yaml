---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:

  # Namespaces
  - ../../base/core/namespaces/sealed-secrets

  # Operatorgroups
  - ../../base/operators.coreos.com/operatorgroups/sealed-secrets

  # Subscriptions
  - ../../base/operators.coreos.com/subscriptions/sealed-secrets

  # Cluster role
  - ../../base/rbac.authorization.k8s.io/clusterroles/sealed-secrets